# Repo-local tool configuration ONLY. This product declares NO package: per
# conventions/no-internal-packages.md, a package means a separate repo with a valid PyPI
# project, consumed as a dependency. The data-pipeline/ folder is scripts, invoked by path.
# DispatchLab, the offline engine package (instantiated from the CAOS product-repo archetype, ADR-0057).
# The DEFAULT pipeline is numpy-only (builds the replay layer from committed artifacts); the heavy lane (the Node DES
# dataset + torch policy training) lives in requirements-precompute.txt and is documented in docs/frameworks/.
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py311"
extend-exclude = ["data-pipeline/pipeline/science"]

[tool.ruff.lint]
ignore = ["E741"]
