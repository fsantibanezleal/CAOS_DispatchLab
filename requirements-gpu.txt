# GPU lane (OPTIONAL, DORMANT), DispatchLab's policy training is tiny (small nets over logged DES decisions,
# seconds on CPU), so no GPU is required. This solution does not require it at the moment.
# torch==2.12.1  --index-url https://download.pytorch.org/whl/cu124
